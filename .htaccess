Options -Indexes
RewriteEngine On

# Abaikan file dan folder yang memang tersedia.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# URL /username diarahkan ke halaman CV publik.
RewriteRule ^([a-zA-Z0-9_]+)/?$ profile.php?username=$1 [L,QSA]
